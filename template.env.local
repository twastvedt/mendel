# Create two versions of this file: '.env.local' and '.env.production.local'. Both will be ignored by git.
# .env.local: Used for local development and in dev container
# .env.production.local: Used for production. (e.g. docker/publish.sh)

# Public site URL
VITE_SERVER_URL=http://localhost:3000
DB_HOST=db
DB_PORT=5432
